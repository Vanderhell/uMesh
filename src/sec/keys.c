#include "keys.h"

/* Stub — will be implemented in Step 6 */
void keys_derive(const uint8_t *master_key,
                 uint8_t        net_id,
                 uint8_t       *enc_key,
                 uint8_t       *auth_key)
{
    UMESH_UNUSED(master_key);
    UMESH_UNUSED(net_id);
    UMESH_UNUSED(enc_key);
    UMESH_UNUSED(auth_key);
}
